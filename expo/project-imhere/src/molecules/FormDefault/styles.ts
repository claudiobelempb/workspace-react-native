import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 36,
    marginBottom: 42,
    flexDirection: 'row',
    alignItems: 'center'
  },
  content: {}
});
